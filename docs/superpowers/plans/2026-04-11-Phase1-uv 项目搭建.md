# Phase 1: uv 项目搭建实现计划

**创建日期**: 2026-04-11  
**总工期**: 6 天  
**目标**: 完成项目基础框架搭建，实现 Hello World Streamlit 应用

---

## 任务概览

| 任务 ID | 任务名称 | 工期 | 优先级 | 状态 |
|--------|---------|------|--------|------|
| 1.1 | 创建 uv 项目 | 0.5 天 | P0 | ⏳ 待开始 |
| 1.2 | 安装依赖 | 0.5 天 | P0 | ⏳ 待开始 |
| 1.3 | 创建目录结构 | 0.5 天 | P0 | ⏳ 待开始 |
| 1.4 | DuckDB Schema | 1 天 | P0 | ⏳ 待开始 |
| 1.5 | Streamlit 框架 | 1 天 | P0 | ⏳ 待开始 |
| 1.6 | 页面导航 | 1 天 | P0 | ⏳ 待开始 |
| 1.7 | 示例数据导入 | 1 天 | P0 | ⏳ 待开始 |

---

## 任务 1.1: 创建 uv 项目

### 任务描述
初始化 uv 项目，创建 `pyproject.toml` 和基础配置文件。

### 前置条件
- 已安装 uv (`curl -LsSf https://astral.sh/uv/install.sh | sh`)
- Python 3.12 可用
- 工作目录：`/Users/limingkai/nas/project/edu_info`

### 实现步骤 (TDD)

#### Step 1: 验证环境

**测试命令**:
```bash
# 验证 uv 已安装
uv --version

# 验证 Python 版本
python --version

# 验证当前目录
pwd
```

#### Step 2: 初始化 uv 项目

**实现命令**:
```bash
# 初始化 uv 项目（如已有 pyproject.toml 则跳过）
uv init edu-info --no-readme

# 进入项目目录
cd edu-info

# 设置 Python 版本
echo "3.12" > .python-version
uv python pin 3.12
```

#### Step 3: 创建 pyproject.toml 配置

**实现代码**:

编辑 `pyproject.toml`，添加完整配置：

```toml
[project]
name = "edu-info"
version = "0.1.0"
description = "升学规划咨询系统 - 基于国家战略的个性化升学规划工具"
readme = "README.md"
requires-python = ">=3.12"
license = { text = "MIT" }
authors = [
    { name = "Limingkai", email = "your.email@example.com" }
]
keywords = [
    "education",
    "career-planning",
    "university-admission",
    "data-analysis",
]
classifiers = [
    "Development Status :: 3 - Alpha",
    "Intended Audience :: Education",
    "License :: OSI Approved :: MIT License",
    "Programming Language :: Python :: 3.12",
    "Topic :: Education",
]

# 运行时依赖（Phase 1 最小集）
dependencies = [
    "streamlit>=1.32.0",
    "duckdb>=1.0.0",
    "pandas>=2.2.0",
    "pydantic>=2.6.0",
]

# 开发依赖
[project.optional-dependencies]
dev = [
    "pytest>=8.0.0",
    "pytest-cov>=4.1.0",
    "ruff>=0.3.0",
    "mypy>=1.8.0",
]

# 项目入口
[project.scripts]
edu-info = "edu_info.main:cli"

# uv 特定配置
[tool.uv]
dev-dependencies = [
    "pytest>=8.0.0",
    "ruff>=0.3.0",
    "mypy>=1.8.0",
]

# 使用清华镜像加速
[[tool.uv.index]]
name = "tsinghua"
url = "https://pypi.tuna.tsinghua.edu.cn/simple"
default = true

# 构建系统
[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/edu_info"]

# Ruff 配置
[tool.ruff]
target-version = "py312"
line-length = 100
select = ["E", "W", "F", "I", "B", "UP"]
ignore = ["E501"]

[tool.ruff.isort]
known-first-party = ["edu_info"]

# Mypy 配置
[tool.mypy]
python_version = "3.12"
strict = true

# Pytest 配置
[tool.pytest.ini_options]
testpaths = ["tests"]
python_files = ["test_*.py"]
addopts = "-v --cov=src/edu_info --cov-report=html"
```

#### Step 4: 创建 README.md

**实现代码**:

```markdown
# 升学规划咨询系统

基于国家战略发展方向，整合教育政策与高校录取数据，为初中/高中学生提供个性化升学规划方案的本地运行工具。

## 快速开始

### 安装依赖

```bash
uv sync
```

### 启动应用

```bash
uv run streamlit run src/edu_info/main.py
```

### 运行测试

```bash
uv run pytest
```

## 开发计划

- ✅ Phase 1: uv 项目搭建
- ⏳ Phase 2: 数据收集
- ⏳ Phase 3: 核心引擎
- ⏳ Phase 4: UI 完善

## 技术栈

- **Python**: 3.12+
- **包管理**: uv
- **UI 框架**: Streamlit
- **数据库**: DuckDB
- **数据处理**: Pandas

## 项目结构

```
edu_info/
├── src/edu_info/       # 源代码
├── tests/              # 测试
├── data/               # 数据目录
└── docs/               # 文档
```

## 许可证

MIT License
```

### Git 提交命令

```bash
# 添加文件
git add .python-version pyproject.toml README.md

# 提交
git commit -m "$(cat <<'EOF'
feat: 初始化 uv 项目结构

- 创建 pyproject.toml 配置文件
- 配置 Python 3.12 版本
- 添加基础依赖（streamlit, duckdb, pandas, pydantic）
- 配置开发工具（pytest, ruff, mypy）
- 使用清华镜像加速
- 添加项目 README

Phase 1.1 完成
EOF
)"
```

### 验收标准

- [ ] `uv --version` 显示版本号
- [ ] `.python-version` 文件存在且内容为 `3.12`
- [ ] `pyproject.toml` 包含完整配置
- [ ] `README.md` 包含项目说明
- [ ] 项目可通过 `uv sync` 初始化

---

## 任务 1.2: 安装依赖

### 任务描述
使用 uv 安装所有必需的依赖包，生成锁定文件。

### 前置条件
- 任务 1.1 已完成
- `pyproject.toml` 已配置

### 实现步骤 (TDD)

#### Step 1: 同步依赖

**测试命令**:
```bash
# 验证依赖可安装
uv sync --dry-run
```

**实现命令**:
```bash
# 安装所有依赖（含开发依赖）
uv sync

# 验证安装成功
uv pip list | grep -E "streamlit|duckdb|pandas|pydantic|pytest|ruff|mypy"
```

#### Step 2: 验证关键包

**测试代码** (`tests/test_dependencies.py`):

```python
"""测试依赖包是否正确安装"""

def test_streamlit_import():
    """测试 Streamlit 可导入"""
    import streamlit
    assert hasattr(streamlit, 'run')

def test_duckdb_import():
    """测试 DuckDB 可导入"""
    import duckdb
    assert hasattr(duckdb, 'connect')

def test_pandas_import():
    """测试 Pandas 可导入"""
    import pandas
    assert hasattr(pandas, 'DataFrame')

def test_pydantic_import():
    """测试 Pydantic 可导入"""
    import pydantic
    assert hasattr(pydantic, 'BaseModel')
```

**运行测试**:
```bash
uv run pytest tests/test_dependencies.py -v
```

#### Step 3: 创建快捷脚本

**实现代码** (`Makefile`):

```makefile
.PHONY: install dev run test lint clean help

# 安装依赖
install:
	uv sync

# 开发模式
dev:
	uv sync --dev

# 启动 Streamlit
run:
	uv run streamlit run src/edu_info/main.py --server.port=8501

# 运行测试
test:
	uv run pytest

# 代码检查
lint:
	uv run ruff check src/edu_info
	uv run mypy src/edu_info

# 格式化代码
format:
	uv run ruff format src/edu_info

# 清理
clean:
	find . -type d -name "__pycache__" -exec rm -rf {} \;
	find . -type f -name "*.pyc" -delete
	find . -type d -name ".pytest_cache" -exec rm -rf {} \;
	find . -type d -name "*.egg-info" -exec rm -rf {} \;

# 帮助
help:
	@echo "可用命令:"
	@echo "  make install   - 安装依赖"
	@echo "  make dev       - 开发模式安装"
	@echo "  make run       - 启动 Streamlit"
	@echo "  make test      - 运行测试"
	@echo "  make lint      - 代码检查"
	@echo "  make format    - 格式化代码"
	@echo "  make clean     - 清理缓存"
```

### Git 提交命令

```bash
# 添加文件
git add uv.lock Makefile tests/test_dependencies.py

# 提交
git commit -m "$(cat <<'EOF'
feat: 安装项目依赖

- 使用 uv sync 安装所有依赖
- 生成 uv.lock 锁定文件
- 创建 Makefile 快捷脚本
- 添加依赖测试用例

验证通过：
✓ streamlit>=1.32.0
✓ duckdb>=1.0.0
✓ pandas>=2.2.0
✓ pydantic>=2.6.0
✓ pytest>=8.0.0
✓ ruff>=0.3.0
✓ mypy>=1.8.0

Phase 1.2 完成
EOF
)"
```

### 验收标准

- [ ] `uv sync` 成功执行
- [ ] `uv.lock` 文件生成
- [ ] 所有依赖可正常导入
- [ ] `tests/test_dependencies.py` 测试通过
- [ ] `make install` 可用

---

## 任务 1.3: 创建目录结构

### 任务描述
创建完整的项目目录结构，包含源代码、测试、数据等目录。

### 前置条件
- 任务 1.2 已完成
- 依赖已安装

### 实现步骤 (TDD)

#### Step 1: 创建目录结构测试

**测试代码** (`tests/test_project_structure.py`):

```python
"""测试项目目录结构是否正确"""
import os
from pathlib import Path

PROJECT_ROOT = Path(__file__).parent.parent
SRC_DIR = PROJECT_ROOT / "src" / "edu_info"

def test_src_directory_exists():
    """测试 src/edu_info 目录存在"""
    assert (SRC_DIR).exists(), f"{SRC_DIR} 目录不存在"

def test_init_file_exists():
    """测试 __init__.py 存在"""
    assert (SRC_DIR / "__init__.py").exists()

def test_main_file_exists():
    """测试 main.py 存在"""
    assert (SRC_DIR / "main.py").exists()

def test_core_directory_exists():
    """测试 core 目录存在"""
    assert (SRC_DIR / "core").exists()
    assert (SRC_DIR / "core" / "__init__.py").exists()

def test_models_directory_exists():
    """测试 models 目录存在"""
    assert (SRC_DIR / "models").exists()
    assert (SRC_DIR / "models" / "__init__.py").exists()

def test_database_directory_exists():
    """测试 database 目录存在"""
    assert (SRC_DIR / "database").exists()
    assert (SRC_DIR / "database" / "__init__.py").exists()

def test_ui_directory_exists():
    """测试 ui 目录存在"""
    assert (SRC_DIR / "ui").exists()
    assert (SRC_DIR / "ui" / "__init__.py").exists()

def test_data_directory_exists():
    """测试 data 目录存在"""
    assert (PROJECT_ROOT / "data").exists()

def test_tests_directory_exists():
    """测试 tests 目录存在"""
    assert (PROJECT_ROOT / "tests").exists()
```

**运行测试** (预期失败):
```bash
uv run pytest tests/test_project_structure.py -v
```

#### Step 2: 创建目录结构

**实现命令**:
```bash
# 创建源代码目录
mkdir -p src/edu_info/{core,models,database,services,ui/pages,ui/components,utils,data}

# 创建 __init__.py 文件
touch src/edu_info/__init__.py
touch src/edu_info/core/__init__.py
touch src/edu_info/models/__init__.py
touch src/edu_info/database/__init__.py
touch src/edu_info/services/__init__.py
touch src/edu_info/ui/__init__.py
touch src/edu_info/ui/pages/__init__.py
touch src/edu_info/ui/components/__init__.py
touch src/edu_info/utils/__init__.py

# 创建数据目录
mkdir -p data/{duckdb,imports/{scores,policies},exports/{reports,excel}}

# 创建脚本目录
mkdir -p scripts

# 创建 notebooks 目录
mkdir -p notebooks
```

#### Step 3: 创建模块占位文件

**实现代码** (`src/edu_info/__init__.py`):

```python
"""升学规划咨询系统 - 核心包"""

__version__ = "0.1.0"
__author__ = "Limingkai"

from edu_info.utils.logger import setup_logger

# 初始化日志
logger = setup_logger(__name__)
```

**实现代码** (`src/edu_info/utils/logger.py`):

```python
"""日志配置模块"""
import logging
import sys
from typing import Optional

def setup_logger(name: str, level: str = "INFO") -> logging.Logger:
    """
    设置并返回日志记录器
    
    Args:
        name: 日志记录器名称
        level: 日志级别
        
    Returns:
        配置好的日志记录器
    """
    logger = logging.getLogger(name)
    logger.setLevel(getattr(logging, level.upper()))
    
    # 避免重复添加 handler
    if logger.handlers:
        return logger
    
    # 创建控制台 handler
    handler = logging.StreamHandler(sys.stdout)
    handler.setLevel(logging.DEBUG)
    
    # 创建格式化器
    formatter = logging.Formatter(
        "%(asctime)s - %(name)s - %(levelname)s - %(message)s",
        datefmt="%Y-%m-%d %H:%M:%S"
    )
    handler.setFormatter(formatter)
    
    # 添加 handler
    logger.addHandler(handler)
    
    return logger
```

**实现代码** (`src/edu_info/main.py`):

```python
"""Streamlit 应用主入口"""
import streamlit as st
from edu_info import __version__

def main():
    """主函数"""
    st.set_page_config(
        page_title="升学规划咨询系统",
        page_icon="🎓",
        layout="wide",
        initial_sidebar_state="expanded"
    )
    
    st.title("🎓 升学规划咨询系统")
    st.markdown(f"**版本**: {__version__}")
    st.markdown("---")
    
    st.success("✅ 系统初始化成功！")
    
    st.markdown("""
    ## 欢迎使用升学规划咨询系统
    
    本系统基于国家战略发展方向，整合教育政策与高校录取数据，
    为初中/高中学生提供个性化升学规划方案。
    
    ### 核心功能
    
    - 📝 学生档案管理
    - 📊 数据导入与分析
    - 🎯 升学路线规划
    - 📈 结果展示与可视化
    
    ### 快速开始
    
    1. 在左侧边栏选择功能模块
    2. 创建学生档案
    3. 导入分数线数据
    4. 运行规划分析
    5. 查看规划方案
    
    ---
    
    *Phase 1: 项目框架搭建完成*
    """)

if __name__ == "__main__":
    main()
```

### Git 提交命令

```bash
# 添加文件
git add src/ tests/test_project_structure.py Makefile

# 提交
git commit -m "$(cat <<'EOF'
feat: 创建完整项目目录结构

目录结构:
✓ src/edu_info/ - 主包
✓ src/edu_info/core/ - 核心引擎
✓ src/edu_info/models/ - 数据模型
✓ src/edu_info/database/ - 数据访问
✓ src/edu_info/services/ - 业务服务
✓ src/edu_info/ui/ - UI 组件
✓ src/edu_info/utils/ - 工具函数
✓ data/ - 数据目录
✓ scripts/ - 脚本工具
✓ notebooks/ - Jupyter notebooks

实现:
- 添加日志配置模块
- 创建 Streamlit 主入口
- 所有模块 __init__.py 就绪

Phase 1.3 完成
EOF
)"
```

### 验收标准

- [ ] 所有目录创建成功
- [ ] `tests/test_project_structure.py` 测试通过
- [ ] `src/edu_info/main.py` 可运行
- [ ] `src/edu_info/__init__.py` 可导入
- [ ] 日志系统正常工作

---

## 任务 1.4: DuckDB Schema

### 任务描述
创建 DuckDB 数据库 Schema，定义所有数据表结构。

### 前置条件
- 任务 1.3 已完成
- 目录结构已创建

### 实现步骤 (TDD)

#### Step 1: 创建 Schema 测试

**测试代码** (`tests/test_database_schema.py`):

```python
"""测试 DuckDB 数据库 Schema"""
import pytest
import duckdb
from pathlib import Path
from edu_info.database.schema import get_schema_sql, init_database

@pytest.fixture
def test_db_path(tmp_path):
    """测试数据库路径"""
    return tmp_path / "test.db"

@pytest.fixture
def test_conn(test_db_path):
    """测试数据库连接"""
    conn = duckdb.connect(str(test_db_path))
    yield conn
    conn.close()

def test_schema_sql_returns_string():
    """测试 get_schema_sql 返回字符串"""
    schema = get_schema_sql()
    assert isinstance(schema, str)
    assert "CREATE TABLE" in schema
    assert "universities" in schema

def test_init_database_creates_tables(test_conn):
    """测试 init_database 创建所有表"""
    # 初始化数据库
    init_database(test_conn)
    
    # 查询所有表
    result = test_conn.execute(
        "SELECT table_name FROM information_schema.tables WHERE table_schema='main'"
    ).fetchall()
    
    table_names = [row[0] for row in result]
    
    # 验证所有表已创建
    expected_tables = [
        "universities", "majors", "admission_scores",
        "students", "planning_routes", "policies"
    ]
    for table in expected_tables:
        assert table in table_names, f"表 {table} 未创建"

def test_universities_table_structure(test_conn):
    """测试 universities 表结构"""
    init_database(test_conn)
    
    # 查询表结构
    result = test_conn.execute("DESCRIBE universities").fetchall()
    columns = [row[0] for row in result]
    
    expected_columns = [
        "id", "name", "code", "location", "type",
        "is_985", "is_211", "is_double_first_class"
    ]
    for col in expected_columns:
        assert col in columns, f"列 {col} 不存在"

def test_students_table_structure(test_conn):
    """测试 students 表结构"""
    init_database(test_conn)
    
    result = test_conn.execute("DESCRIBE students").fetchall()
    columns = [row[0] for row in result]
    
    expected_columns = [
        "id", "name", "student_code", "grade", "category",
        "total_score", "interests", "specialities"
    ]
    for col in expected_columns:
        assert col in columns, f"列 {col} 不存在"

def test_database_accepts_sample_data(test_conn):
    """测试数据库可接受示例数据"""
    init_database(test_conn)
    
    # 插入示例高校数据
    test_conn.execute("""
        INSERT INTO universities (name, code, location, type, is_985, is_211)
        VALUES ('清华大学', '10003', '北京', '综合', TRUE, TRUE)
    """)
    
    # 查询验证
    result = test_conn.execute(
        "SELECT name FROM universities WHERE code='10003'"
    ).fetchone()
    
    assert result is not None
    assert result[0] == "清华大学"
```

#### Step 2: 实现 Schema 模块

**实现代码** (`src/edu_info/database/schema.py`):

```python
"""DuckDB 数据库 Schema 定义"""
from typing import Optional
import duckdb
from pathlib import Path

def get_schema_sql() -> str:
    """
    返回完整的数据库 Schema SQL
    
    Returns:
        包含所有表创建语句的 SQL 字符串
    """
    return """
    -- 高校数据表
    CREATE TABLE IF NOT EXISTS universities (
        id INTEGER PRIMARY KEY,
        name VARCHAR(100) NOT NULL,
        code VARCHAR(10) UNIQUE,
        location VARCHAR(50),
        type VARCHAR(20),
        is_985 BOOLEAN DEFAULT FALSE,
        is_211 BOOLEAN DEFAULT FALSE,
        is_double_first_class BOOLEAN DEFAULT FALSE,
        project_type VARCHAR(20),
        website VARCHAR(200),
        description TEXT,
        created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
        updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
    );
    
    -- 专业数据表
    CREATE TABLE IF NOT EXISTS majors (
        id INTEGER PRIMARY KEY,
        university_id INTEGER REFERENCES universities(id),
        name VARCHAR(100) NOT NULL,
        code VARCHAR(10),
        category VARCHAR(50),
        degree VARCHAR(20),
        duration INTEGER DEFAULT 4,
        discipline_rank VARCHAR(5),
        is_national_key BOOLEAN DEFAULT FALSE,
        created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
    );
    
    -- 录取分数线表
    CREATE TABLE IF NOT EXISTS admission_scores (
        id INTEGER PRIMARY KEY,
        university_id INTEGER REFERENCES universities(id),
        major_id INTEGER REFERENCES majors(id),
        year INTEGER NOT NULL,
        province VARCHAR(50) NOT NULL,
        category VARCHAR(20) NOT NULL,
        min_score INTEGER,
        max_score INTEGER,
        avg_score INTEGER,
        min_rank INTEGER,
        max_rank INTEGER,
        plan_count INTEGER,
        actual_count INTEGER,
        batch VARCHAR(20),
        created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
        UNIQUE(university_id, major_id, year, province, category)
    );
    
    -- 学生档案表
    CREATE TABLE IF NOT EXISTS students (
        id INTEGER PRIMARY KEY,
        name VARCHAR(50) NOT NULL,
        student_code VARCHAR(20) UNIQUE,
        gender VARCHAR(10),
        birth_date DATE,
        grade VARCHAR(20),
        school VARCHAR(100),
        city VARCHAR(50),
        category VARCHAR(20),
        total_score DECIMAL(5,2),
        ranking INTEGER,
        chinese_score DECIMAL(5,2),
        math_score DECIMAL(5,2),
        english_score DECIMAL(5,2),
        other_scores JSON,
        interests JSON,
        specialities JSON,
        awards JSON,
        family_budget DECIMAL(10,2),
        preferred_locations JSON,
        constraints JSON,
        planning_status VARCHAR(20) DEFAULT '未开始',
        last_plan_date TIMESTAMP,
        created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
        updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
    );
    
    -- 规划路线表
    CREATE TABLE IF NOT EXISTS planning_routes (
        id INTEGER PRIMARY KEY,
        student_id INTEGER REFERENCES students(id),
        route_name VARCHAR(100),
        route_type VARCHAR(50),
        stages JSON,
        requirements JSON,
        timeline JSON,
        match_score DECIMAL(5,2),
        match_details JSON,
        target_universities JSON,
        feasibility_score DECIMAL(5,2),
        success_probability DECIMAL(5,2),
        risk_level VARCHAR(20),
        is_primary BOOLEAN DEFAULT FALSE,
        status VARCHAR(20) DEFAULT '草稿',
        created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
        updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
    );
    
    -- 政策文件表
    CREATE TABLE IF NOT EXISTS policies (
        id INTEGER PRIMARY KEY,
        title VARCHAR(200) NOT NULL,
        doc_number VARCHAR(50),
        issuer VARCHAR(100),
        issue_date DATE,
        effective_date DATE,
        level VARCHAR(20),
        category VARCHAR(50),
        region VARCHAR(50),
        file_path VARCHAR(300),
        content TEXT,
        summary TEXT,
        keywords JSON,
        related_routes JSON,
        related_majors JSON,
        created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
    );
    
    -- 创建索引
    CREATE INDEX IF NOT EXISTS idx_univ_type ON universities(is_985, is_211);
    CREATE INDEX IF NOT EXISTS idx_univ_location ON universities(location);
    CREATE INDEX IF NOT EXISTS idx_major_univ ON majors(university_id);
    CREATE INDEX IF NOT EXISTS idx_major_category ON majors(category);
    CREATE INDEX IF NOT EXISTS idx_score_year ON admission_scores(year);
    CREATE INDEX IF NOT EXISTS idx_score_univ ON admission_scores(university_id);
    CREATE INDEX IF NOT EXISTS idx_score_rank ON admission_scores(min_rank);
    CREATE INDEX IF NOT EXISTS idx_policy_category ON policies(category);
    CREATE INDEX IF NOT EXISTS idx_policy_level ON policies(level);
    """

def init_database(conn: duckdb.DuckDBPyConnection) -> None:
    """
    初始化数据库，创建所有表
    
    Args:
        conn: DuckDB 连接对象
    """
    schema_sql = get_schema_sql()
    conn.execute(schema_sql)

def create_database(db_path: str) -> duckdb.DuckDBPyConnection:
    """
    创建数据库连接并初始化
    
    Args:
        db_path: 数据库文件路径
        
    Returns:
        DuckDB 连接对象
    """
    # 确保目录存在
    db_file = Path(db_path)
    db_file.parent.mkdir(parents=True, exist_ok=True)
    
    # 创建连接
    conn = duckdb.connect(str(db_file))
    
    # 初始化 Schema
    init_database(conn)
    
    return conn

def get_connection(db_path: str) -> duckdb.DuckDBPyConnection:
    """
    获取数据库连接
    
    Args:
        db_path: 数据库文件路径
        
    Returns:
        DuckDB 连接对象
    """
    db_file = Path(db_path)
    
    if not db_file.exists():
        return create_database(db_path)
    
    return duckdb.connect(str(db_file))
```

**实现代码** (`src/edu_info/database/__init__.py`):

```python
"""数据库模块"""
from edu_info.database.schema import (
    get_schema_sql,
    init_database,
    create_database,
    get_connection
)

__all__ = [
    "get_schema_sql",
    "init_database",
    "create_database",
    "get_connection"
]
```

#### Step 3: 创建数据库初始化脚本

**实现代码** (`scripts/setup_db.py`):

```python
#!/usr/bin/env python3
"""初始化数据库脚本"""
import sys
from pathlib import Path

# 添加 src 到路径
sys.path.insert(0, str(Path(__file__).parent.parent / "src"))

from edu_info.database import create_database
from edu_info.utils.logger import setup_logger

logger = setup_logger(__name__)

def main():
    """主函数"""
    # 数据库路径
    db_path = Path(__file__).parent.parent / "data" / "duckdb" / "edu_planning.db"
    
    logger.info(f"创建数据库：{db_path}")
    
    # 创建数据库
    conn = create_database(str(db_path))
    
    # 验证表已创建
    tables = conn.execute(
        "SELECT table_name FROM information_schema.tables WHERE table_schema='main'"
    ).fetchall()
    
    logger.info(f"成功创建 {len(tables)} 个表:")
    for table in tables:
        logger.info(f"  - {table[0]}")
    
    conn.close()
    logger.info("数据库初始化完成 ✅")

if __name__ == "__main__":
    main()
```

### Git 提交命令

```bash
# 添加文件
git add src/edu_info/database/ scripts/setup_db.py tests/test_database_schema.py

# 提交
git commit -m "$(cat <<'EOF'
feat: 实现 DuckDB 数据库 Schema

数据表:
✓ universities - 高校数据表 (985/211/双一流)
✓ majors - 专业数据表
✓ admission_scores - 录取分数线表
✓ students - 学生档案表
✓ planning_routes - 规划路线表
✓ policies - 政策文件表

功能:
- get_schema_sql() - 返回完整 Schema SQL
- init_database() - 初始化数据库表
- create_database() - 创建并初始化数据库
- get_connection() - 获取数据库连接

索引:
- 创建所有必要的索引
- 优化查询性能

脚本:
- scripts/setup_db.py - 数据库初始化脚本

Phase 1.4 完成
EOF
)"
```

### 验收标准

- [ ] `tests/test_database_schema.py` 所有测试通过
- [ ] `scripts/setup_db.py` 可成功运行
- [ ] 所有 6 个表创建成功
- [ ] 索引创建成功
- [ ] 可插入示例数据

---

## 任务 1.5: Streamlit 框架

### 任务描述
创建 Streamlit 应用框架，实现主页面和基础配置。

### 前置条件
- 任务 1.4 已完成
- 数据库 Schema 已定义

### 实现步骤 (TDD)

#### Step 1: 创建 Streamlit 测试

**测试代码** (`tests/test_streamlit_app.py`):

```python
"""测试 Streamlit 应用"""
import pytest
import subprocess
import time
import requests
from pathlib import Path

def test_streamlit_app_exists():
    """测试 main.py 存在"""
    main_path = Path(__file__).parent.parent / "src" / "edu_info" / "main.py"
    assert main_path.exists(), "main.py 不存在"

def test_streamlit_app_syntax():
    """测试 main.py 语法正确"""
    main_path = Path(__file__).parent.parent / "src" / "edu_info" / "main.py"
    
    # 使用 Python 检查语法
    result = subprocess.run(
        ["python", "-m", "py_compile", str(main_path)],
        capture_output=True,
        text=True
    )
    
    assert result.returncode == 0, f"语法错误：{result.stderr}"

def test_streamlit_imports_streamlit():
    """测试 main.py 导入 streamlit"""
    main_path = Path(__file__).parent.parent / "src" / "edu_info" / "main.py"
    content = main_path.read_text()
    
    assert "import streamlit" in content or "from streamlit" in content

@pytest.mark.skip(reason="需要启动 Streamlit 服务")
def test_streamlit_app_runs():
    """测试 Streamlit 应用可启动（集成测试）"""
    # 启动 Streamlit
    process = subprocess.Popen(
        ["uv", "run", "streamlit", "run", "src/edu_info/main.py", "--server.port=8501", "--server.headless=true"],
        stdout=subprocess.PIPE,
        stderr=subprocess.PIPE
    )
    
    # 等待启动
    time.sleep(5)
    
    try:
        # 检查服务是否响应
        response = requests.get("http://localhost:8501", timeout=5)
        assert response.status_code == 200
    finally:
        process.terminate()
        process.wait()
```

#### Step 2: 完善 Streamlit 主入口

**实现代码** (`src/edu_info/main.py`):

```python
"""Streamlit 应用主入口"""
import streamlit as st
from pathlib import Path
from edu_info import __version__
from edu_info.utils.logger import setup_logger
from edu_info.database import get_connection

# 初始化日志
logger = setup_logger(__name__)

def init_page_config():
    """初始化页面配置"""
    st.set_page_config(
        page_title="升学规划咨询系统",
        page_icon="🎓",
        layout="wide",
        initial_sidebar_state="expanded",
        menu_items={
            'Get Help': 'https://github.com/your-repo/edu-info',
            'Report a bug': 'https://github.com/your-repo/edu-info/issues',
            'About': f"# 升学规划咨询系统 v{__version__}"
        }
    )

def init_sidebar():
    """初始化侧边栏"""
    with st.sidebar:
        st.title("🎓 升学规划")
        st.markdown("---")
        
        # 导航菜单
        page = st.radio(
            "导航",
            ["首页", "学生档案管理", "数据导入", "规划分析", "结果展示", "数据可视化"],
            index=0
        )
        
        st.markdown("---")
        st.info(f"版本：{__version__}")
        
        # 数据库状态
        db_path = Path(__file__).parent.parent.parent / "data" / "duckdb" / "edu_planning.db"
        if db_path.exists():
            st.success("✅ 数据库已连接")
        else:
            st.warning("⚠️ 数据库未初始化")
    
    return page

def render_home():
    """渲染首页"""
    st.title("🎓 升学规划咨询系统")
    st.markdown(f"**版本**: {__version__}")
    st.markdown("---")
    
    st.success("✅ 系统初始化成功！")
    
    col1, col2, col3 = st.columns(3)
    
    with col1:
        st.markdown("""
        ### 📝 核心功能
        
        - 学生档案管理
        - 数据导入与分析
        - 升学路线规划
        - 结果展示
        """)
    
    with col2:
        st.markdown("""
        ### 🎯 升学途径
        
        - 普通高考
        - 强基计划
        - 科技特长生
        - 艺术特长生
        - 体育特长生
        """)
    
    with col3:
        st.markdown("""
        ### 📊 数据覆盖
        
        - 985 高校：39 所
        - 211 高校：116 所
        - 双一流：147 所
        - 近 5 年分数线
        """)
    
    st.markdown("---")
    
    st.markdown("""
    ## 快速开始
    
    1. **创建学生档案** - 在"学生档案管理"页面添加学生信息
    2. **导入分数线数据** - 在"数据导入"页面上传 Excel 数据
    3. **运行规划分析** - 在"规划分析"页面生成规划方案
    4. **查看结果** - 在"结果展示"页面查看三档目标高校
    
    ---
    
    *基于国家战略发展方向，为每个学生提供个性化升学规划方案*
    """)

def render_placeholder_page(page_name: str, icon: str):
    """渲染占位页面"""
    st.title(f"{icon} {page_name}")
    st.markdown("---")
    
    st.info(f"{page_name} 功能开发中...")
    
    st.markdown("""
    ### 功能预告
    
    该页面将在后续 Phase 中实现：
    
    - Phase 2: 数据收集
    - Phase 3: 核心引擎
    - Phase 4: UI 完善
    
    敬请期待！
    """)

def main():
    """主函数"""
    # 初始化配置
    init_page_config()
    
    # 初始化侧边栏
    page = init_sidebar()
    
    # 根据选择渲染页面
    if page == "首页":
        render_home()
    elif page == "学生档案管理":
        render_placeholder_page("学生档案管理", "📝")
    elif page == "数据导入":
        render_placeholder_page("数据导入", "📊")
    elif page == "规划分析":
        render_placeholder_page("规划分析", "🎯")
    elif page == "结果展示":
        render_placeholder_page("结果展示", "📈")
    elif page == "数据可视化":
        render_placeholder_page("数据可视化", "📉")

if __name__ == "__main__":
    main()
```

#### Step 3: 创建 Streamlit 配置

**实现代码** (`.streamlit/config.toml`):

```toml
[theme]
primaryColor = "#3B82F6"
backgroundColor = "#FFFFFF"
secondaryBackgroundColor = "#F3F4F6"
textColor = "#1F2937"
font = "sans serif"

[server]
port = 8501
headless = true
enableCORS = false
enableXsrfProtection = true

[browser]
gatherUsageStats = false
```

### Git 提交命令

```bash
# 添加文件
git add src/edu_info/main.py .streamlit/config.toml tests/test_streamlit_app.py

# 提交
git commit -m "$(cat <<'EOF'
feat: 实现 Streamlit 应用框架

主入口:
✓ src/edu_info/main.py - Streamlit 主应用

功能:
- 页面配置初始化
- 侧边栏导航菜单
- 首页展示
- 占位页面（后续实现）

配置:
- .streamlit/config.toml - 主题和服务器配置
- 蓝色主题配色
- 端口 8501

导航:
- 首页
- 学生档案管理（待实现）
- 数据导入（待实现）
- 规划分析（待实现）
- 结果展示（待实现）
- 数据可视化（待实现）

Phase 1.5 完成
EOF
)"
```

### 验收标准

- [ ] `tests/test_streamlit_app.py` 测试通过
- [ ] `uv run streamlit run src/edu_info/main.py` 可启动
- [ ] 页面可正常访问（http://localhost:8501）
- [ ] 侧边栏导航可切换页面
- [ ] 首页内容正常显示

---

## 任务 1.6: 页面导航

### 任务描述
实现多页面导航系统，创建 Streamlit Pages 结构。

### 前置条件
- 任务 1.5 已完成
- Streamlit 主框架已搭建

### 实现步骤 (TDD)

#### Step 1: 创建导航测试

**测试代码** (`tests/test_page_navigation.py`):

```python
"""测试页面导航系统"""
import pytest
from pathlib import Path

UI_DIR = Path(__file__).parent.parent / "src" / "edu_info" / "ui" / "pages"

def test_ui_pages_directory_exists():
    """测试 ui/pages 目录存在"""
    assert UI_DIR.exists()

def test_page_files_exist():
    """测试所有页面文件存在"""
    expected_pages = [
        "01_学生档案管理.py",
        "02_数据导入.py",
        "03_规划分析.py",
        "04_结果展示.py",
        "05_数据可视化.py"
    ]
    
    for page in expected_pages:
        page_path = UI_DIR / page
        assert page_path.exists(), f"页面文件 {page} 不存在"

def test_page_has_streamlit_import():
    """测试每个页面都导入 streamlit"""
    page_files = list(UI_DIR.glob("*.py"))
    
    for page_file in page_files:
        content = page_file.read_text()
        assert "import streamlit" in content or "from streamlit" in content, \
            f"{page_file.name} 未导入 streamlit"

def test_page_has_main_function():
    """测试每个页面都有 main 函数"""
    page_files = list(UI_DIR.glob("*.py"))
    
    for page_file in page_files:
        content = page_file.read_text()
        assert "def main():" in content, f"{page_file.name} 缺少 main 函数"
```

#### Step 2: 创建页面文件

**实现代码** (`src/edu_info/ui/pages/01_学生档案管理.py`):

```python
"""学生档案管理页面"""
import streamlit as st

def main():
    """主函数"""
    st.title("📝 学生档案管理")
    st.markdown("---")
    
    st.info("功能开发中... 将在 Phase 4 实现")
    
    # 占位内容
    col1, col2 = st.columns(2)
    
    with col1:
        st.markdown("""
        ### 功能列表
        
        - ✅ 创建学生档案
        - ✅ 编辑学生信息
        - ✅ 删除学生档案
        - ✅ 查看档案列表
        - ✅ 搜索学生
        """)
    
    with col2:
        st.markdown("""
        ### 档案信息
        
        - 基本信息（姓名、年级、学校）
        - 成绩信息（总分、单科、位次）
        - 兴趣特长（兴趣列表、奖项）
        - 家庭情况（预算、偏好）
        """)
```

**实现代码** (`src/edu_info/ui/pages/02_数据导入.py`):

```python
"""数据导入页面"""
import streamlit as st

def main():
    """主函数"""
    st.title("📊 数据导入")
    st.markdown("---")
    
    st.info("功能开发中... 将在 Phase 2 实现")
    
    st.markdown("""
    ### 支持的数据类型
    
    1. **高校分数线 Excel**
       - 格式：.xlsx / .csv
       - 内容：学校名称、专业、分数、位次
    
    2. **政策文件**
       - 格式：.pdf / .docx / .txt
       - 内容：政策文本、发布日期
    
    3. **学生数据批量导入**
       - 格式：.xlsx / .json
       - 内容：学生档案批量导入
    """)
    
    # 占位上传组件
    st.file_uploader("上传 Excel 文件（占位）", type=["xlsx", "csv"], disabled=True)
```

**实现代码** (`src/edu_info/ui/pages/03_规划分析.py`):

```python
"""规划分析页面"""
import streamlit as st

def main():
    """主函数"""
    st.title("🎯 规划分析")
    st.markdown("---")
    
    st.info("功能开发中... 将在 Phase 3 实现")
    
    st.markdown("""
    ### 规划引擎
    
    1. **路线穷举器**
       - 生成 50-100 条升学路线
       - 包含普通高考、特长生、强基计划等
    
    2. **匹配引擎**
       - 兴趣匹配（40%）
       - 能力匹配（25%）
       - 经济匹配（20%）
       - 时间匹配（10%）
       - 地域匹配（5%）
    
    3. **目标生成器**
       - 高目标（冲刺）
       - 中目标（稳妥）
       - 低目标（保底）
    
    4. **可行性评估**
       - 录取概率计算
       - 风险评估
       - 备选方案
    """)
```

**实现代码** (`src/edu_info/ui/pages/04_结果展示.py`):

```python
"""结果展示页面"""
import streamlit as st

def main():
    """主函数"""
    st.title("📈 结果展示")
    st.markdown("---")
    
    st.info("功能开发中... 将在 Phase 4 实现")
    
    st.markdown("""
    ### 展示内容
    
    1. **规划方案总览**
       - 推荐路线列表
       - 匹配度评分
       - 可行性分析
    
    2. **三档目标高校**
       - 高目标（冲刺院校）
       - 中目标（稳妥院校）
       - 低目标（保底院校）
    
    3. **详细分析**
       - 历年分数线对比
       - 录取概率
       - 专业推荐
    
    4. **导出功能**
       - 导出 Excel
       - 导出 PDF 报告（P1）
    """)
```

**实现代码** (`src/edu_info/ui/pages/05_数据可视化.py`):

```python
"""数据可视化页面"""
import streamlit as st

def main():
    """主函数"""
    st.title("📉 数据可视化")
    st.markdown("---")
    
    st.info("功能开发中... 将在 Phase 4 实现")
    
    st.markdown("""
    ### 可视化图表
    
    1. **分数线趋势图**
       - 近 5 年分数线变化
       - 位次变化趋势
    
    2. **学生能力雷达图**
       - 学科能力
       - 综合素质
    
    3. **高校分布地图**
       - 地域分布
       - 高校层次
    
    4. **专业热度图**
       - 热门专业
       - 就业趋势
    
    5. **方案对比图**
       - 多方案对比
       - 优劣势分析
    """)
```

#### Step 3: 更新主应用使用 Pages

**实现代码** (`src/edu_info/main.py`):

```python
"""Streamlit 应用主入口"""
import streamlit as st
from pathlib import Path
from edu_info import __version__
from edu_info.utils.logger import setup_logger

# 初始化日志
logger = setup_logger(__name__)

def init_page_config():
    """初始化页面配置"""
    st.set_page_config(
        page_title="升学规划咨询系统",
        page_icon="🎓",
        layout="wide",
        initial_sidebar_state="expanded"
    )

def main():
    """主函数"""
    init_page_config()
    
    # 标题
    st.title("🎓 升学规划咨询系统")
    st.markdown(f"**版本**: {__version__}")
    st.markdown("---")
    
    st.success("✅ Phase 1: 项目框架搭建完成！")
    
    # 导航
    st.markdown("### 🧭 页面导航")
    
    col1, col2, col3 = st.columns(3)
    
    pages_dir = Path(__file__).parent / "ui" / "pages"
    pages = [
        ("📝 学生档案管理", pages_dir / "01_学生档案管理.py"),
        ("📊 数据导入", pages_dir / "02_数据导入.py"),
        ("🎯 规划分析", pages_dir / "03_规划分析.py"),
        ("📈 结果展示", pages_dir / "04_结果展示.py"),
        ("📉 数据可视化", pages_dir / "05_数据可视化.py"),
    ]
    
    for i, (name, path) in enumerate(pages):
        col = [col1, col2, col3][i % 3]
        with col:
            if st.button(name, key=name, use_container_width=True):
                st.session_state.current_page = path
    
    st.markdown("---")
    
    # 显示当前选择的页面
    if "current_page" in st.session_state:
        page_path = st.session_state.current_page
        if page_path.exists():
            st.markdown(f"### {page_path.stem}")
            # 这里可以动态加载页面，目前仅显示路径
            st.info(f"页面路径：{page_path}")
    
    # 项目进度
    st.markdown("### 📋 项目进度")
    
    progress = st.progress(0)
    progress.progress(0.15, text="Phase 1: uv 项目搭建 - 75% 完成")
    
    st.markdown("""
    ### ✅ 已完成
    
    - [x] 任务 1.1: 创建 uv 项目
    - [x] 任务 1.2: 安装依赖
    - [x] 任务 1.3: 创建目录结构
    - [x] 任务 1.4: DuckDB Schema
    - [x] 任务 1.5: Streamlit 框架
    - [ ] 任务 1.6: 页面导航
    - [ ] 任务 1.7: 示例数据导入
    
    ### 📅 下一步
    
    - Phase 2: 数据收集（第 2-4 周）
    - Phase 3: 核心引擎（第 5-8 周）
    - Phase 4: UI 完善（第 9-11 周）
    """)

if __name__ == "__main__":
    main()
```

### Git 提交命令

```bash
# 添加文件
git add src/edu_info/ui/pages/*.py src/edu_info/main.py

# 提交
git commit -m "$(cat <<'EOF'
feat: 实现多页面导航系统

页面文件:
✓ 01_学生档案管理.py - 学生档案管理（占位）
✓ 02_数据导入.py - 数据导入（占位）
✓ 03_规划分析.py - 规划分析（占位）
✓ 04_结果展示.py - 结果展示（占位）
✓ 05_数据可视化.py - 数据可视化（占位）

功能:
- 所有页面包含 main 函数
- 所有页面导入 streamlit
- 页面占位内容说明

导航:
- 主页面按钮导航
- Session state 管理
- 项目进度展示

Phase 1.6 完成
EOF
)"
```

### 验收标准

- [ ] `tests/test_page_navigation.py` 测试通过
- [ ] 所有 5 个页面文件存在
- [ ] 每个页面都有 `def main():` 函数
- [ ] 每个页面都导入 streamlit
- [ ] 主页面可导航到各子页面

---

## 任务 1.7: 示例数据导入

### 任务描述
创建示例数据并实现数据导入功能。

### 前置条件
- 任务 1.6 已完成
- 数据库已初始化

### 实现步骤 (TDD)

#### Step 1: 创建示例数据测试

**测试代码** (`tests/test_sample_data.py`):

```python
"""测试示例数据导入"""
import pytest
import duckdb
from pathlib import Path
from edu_info.database import create_database
from edu_info.services.data_importer import import_sample_data

@pytest.fixture
def test_db(tmp_path):
    """测试数据库"""
    db_path = tmp_path / "test.db"
    conn = create_database(str(db_path))
    yield conn
    conn.close()

def test_import_sample_universities(test_db):
    """测试导入示例高校数据"""
    import_sample_data(test_db, "universities")
    
    # 查询高校数量
    result = test_db.execute("SELECT COUNT(*) FROM universities").fetchone()
    assert result[0] > 0, "未导入高校数据"
    
    # 验证清华大学存在
    result = test_db.execute(
        "SELECT name FROM universities WHERE name='清华大学'"
    ).fetchone()
    assert result is not None
    assert result[0] == "清华大学"

def test_import_sample_students(test_db):
    """测试导入示例学生数据"""
    import_sample_data(test_db, "students")
    
    # 查询学生数量
    result = test_db.execute("SELECT COUNT(*) FROM students").fetchone()
    assert result[0] > 0, "未导入学生数据"

def test_sample_data_integrity(test_db):
    """测试示例数据完整性"""
    # 导入所有示例数据
    import_sample_data(test_db, "all")
    
    # 验证外键关系
    result = test_db.execute("""
        SELECT COUNT(*) 
        FROM admission_scores a
        JOIN universities u ON a.university_id = u.id
    """).fetchone()
    
    # 所有分数记录都应有对应的高校
    assert result[0] >= 0  # 允许为空（如果没有导入分数数据）
```

#### Step 2: 创建示例数据文件

**实现代码** (`src/edu_info/data/sample_universities.json`):

```json
[
  {
    "id": 1,
    "name": "清华大学",
    "code": "10003",
    "location": "北京",
    "type": "综合",
    "is_985": true,
    "is_211": true,
    "is_double_first_class": true,
    "project_type": "A 类"
  },
  {
    "id": 2,
    "name": "北京大学",
    "code": "10001",
    "location": "北京",
    "type": "综合",
    "is_985": true,
    "is_211": true,
    "is_double_first_class": true,
    "project_type": "A 类"
  },
  {
    "id": 3,
    "name": "东北大学",
    "code": "10145",
    "location": "辽宁沈阳",
    "type": "理工",
    "is_985": true,
    "is_211": true,
    "is_double_first_class": true,
    "project_type": "B 类"
  },
  {
    "id": 4,
    "name": "大连理工大学",
    "code": "10141",
    "location": "辽宁大连",
    "type": "理工",
    "is_985": true,
    "is_211": true,
    "is_double_first_class": true,
    "project_type": "A 类"
  },
  {
    "id": 5,
    "name": "辽宁大学",
    "code": "10140",
    "location": "辽宁沈阳",
    "type": "综合",
    "is_985": false,
    "is_211": true,
    "is_double_first_class": true,
    "project_type": null
  }
]
```

**实现代码** (`src/edu_info/data/sample_students.json`):

```json
[
  {
    "student_code": "S2026001",
    "name": "张小明",
    "gender": "男",
    "grade": "初三",
    "school": "沈阳市实验中学",
    "city": "沈阳",
    "category": "物理类",
    "total_score": 720.5,
    "ranking": 150,
    "chinese_score": 105.0,
    "math_score": 118.0,
    "english_score": 115.0,
    "other_scores": {
      "物理": 95,
      "化学": 92,
      "道德与法治": 95
    },
    "interests": ["编程", "机器人", "数学"],
    "specialities": ["信息学竞赛省二", "机器人竞赛市一"],
    "awards": [
      {"name": "NOIP 普及组省二等奖", "year": 2025},
      {"name": "沈阳市机器人竞赛一等奖", "year": 2025}
    ],
    "family_budget": 100000.0,
    "preferred_locations": ["北京", "上海", "辽宁"],
    "constraints": {
      "经济": "中等",
      "时间": "充足"
    },
    "planning_status": "未开始"
  },
  {
    "student_code": "S2026002",
    "name": "李雨涵",
    "gender": "女",
    "grade": "高一",
    "school": "大连市第二十四中学",
    "city": "大连",
    "category": "历史类",
    "total_score": 680.0,
    "ranking": 300,
    "chinese_score": 120.0,
    "math_score": 105.0,
    "english_score": 125.0,
    "other_scores": {
      "历史": 90,
      "政治": 88,
      "地理": 92
    },
    "interests": ["文学", "历史", "外语"],
    "specialities": ["英语竞赛市一", "作文比赛省三"],
    "awards": [
      {"name": "全国中学生英语能力竞赛市一等奖", "year": 2025},
      {"name": "辽宁省作文比赛三等奖", "year": 2024}
    ],
    "family_budget": 150000.0,
    "preferred_locations": ["北京", "上海", "江苏"],
    "constraints": {
      "经济": "充足",
      "时间": "充足"
    },
    "planning_status": "未开始"
  }
]
```

#### Step 3: 实现数据导入服务

**实现代码** (`src/edu_info/services/data_importer.py`):

```python
"""数据导入服务"""
import json
import duckdb
from pathlib import Path
from typing import Literal
from edu_info.utils.logger import setup_logger

logger = setup_logger(__name__)

def import_sample_data(
    conn: duckdb.DuckDBPyConnection,
    data_type: Literal["universities", "students", "all"] = "all"
) -> None:
    """
    导入示例数据到数据库
    
    Args:
        conn: DuckDB 连接
        data_type: 数据类型（universities/students/all）
    """
    data_dir = Path(__file__).parent.parent / "data"
    
    if data_type in ["universities", "all"]:
        _import_universities(conn, data_dir)
    
    if data_type in ["students", "all"]:
        _import_students(conn, data_dir)
    
    logger.info(f"示例数据导入完成：{data_type}")

def _import_universities(conn: duckdb.DuckDBPyConnection, data_dir: Path) -> None:
    """导入高校数据"""
    sample_file = data_dir / "sample_universities.json"
    
    if not sample_file.exists():
        logger.warning(f"示例高校数据文件不存在：{sample_file}")
        return
    
    with open(sample_file, "r", encoding="utf-8") as f:
        universities = json.load(f)
    
    for univ in universities:
        conn.execute("""
            INSERT OR REPLACE INTO universities (
                id, name, code, location, type,
                is_985, is_211, is_double_first_class, project_type
            ) VALUES (?, ?, ?, ?, ?, ?, ?, ?, ?)
        """, [
            univ["id"],
            univ["name"],
            univ["code"],
            univ["location"],
            univ["type"],
            univ["is_985"],
            univ["is_211"],
            univ["is_double_first_class"],
            univ.get("project_type")
        ])
    
    logger.info(f"导入 {len(universities)} 所高校数据")

def _import_students(conn: duckdb.DuckDBPyConnection, data_dir: Path) -> None:
    """导入学生数据"""
    sample_file = data_dir / "sample_students.json"
    
    if not sample_file.exists():
        logger.warning(f"示例学生数据文件不存在：{sample_file}")
        return
    
    with open(sample_file, "r", encoding="utf-8") as f:
        students = json.load(f)
    
    for student in students:
        conn.execute("""
            INSERT OR REPLACE INTO students (
                student_code, name, gender, grade, school, city,
                category, total_score, ranking,
                chinese_score, math_score, english_score,
                other_scores, interests, specialities, awards,
                family_budget, preferred_locations, constraints,
                planning_status
            ) VALUES (?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?)
        """, [
            student["student_code"],
            student["name"],
            student.get("gender"),
            student["grade"],
            student["school"],
            student["city"],
            student["category"],
            student["total_score"],
            student.get("ranking"),
            student.get("chinese_score"),
            student.get("math_score"),
            student.get("english_score"),
            json.dumps(student.get("other_scores", {})),
            json.dumps(student.get("interests", [])),
            json.dumps(student.get("specialities", [])),
            json.dumps(student.get("awards", [])),
            student.get("family_budget"),
            json.dumps(student.get("preferred_locations", [])),
            json.dumps(student.get("constraints", {})),
            student.get("planning_status", "未开始")
        ])
    
    logger.info(f"导入 {len(students)} 个学生数据")
```

#### Step 4: 创建数据导入脚本

**实现代码** (`scripts/import_sample_data.py`):

```python
#!/usr/bin/env python3
"""导入示例数据脚本"""
import sys
from pathlib import Path

# 添加 src 到路径
sys.path.insert(0, str(Path(__file__).parent.parent / "src"))

from edu_info.database import create_database
from edu_info.services.data_importer import import_sample_data
from edu_info.utils.logger import setup_logger

logger = setup_logger(__name__)

def main():
    """主函数"""
    # 数据库路径
    db_path = Path(__file__).parent.parent / "data" / "duckdb" / "edu_planning.db"
    
    logger.info(f"连接数据库：{db_path}")
    
    # 创建/连接数据库
    conn = create_database(str(db_path))
    
    # 导入示例数据
    import_sample_data(conn, "all")
    
    # 验证导入结果
    univ_count = conn.execute("SELECT COUNT(*) FROM universities").fetchone()[0]
    student_count = conn.execute("SELECT COUNT(*) FROM students").fetchone()[0]
    
    logger.info(f"数据库统计:")
    logger.info(f"  - 高校数量：{univ_count}")
    logger.info(f"  - 学生数量：{student_count}")
    
    conn.close()
    logger.info("示例数据导入完成 ✅")

if __name__ == "__main__":
    main()
```

### Git 提交命令

```bash
# 添加文件
git add src/edu_info/data/ src/edu_info/services/data_importer.py scripts/import_sample_data.py tests/test_sample_data.py

# 提交
git commit -m "$(cat <<'EOF'
feat: 实现示例数据导入

示例数据:
✓ sample_universities.json - 5 所高校（清华/北大/东北大学/大工/辽大）
✓ sample_students.json - 2 个学生档案（张小明/李雨涵）

服务:
✓ data_importer.py - 数据导入服务
  - import_sample_data() - 导入示例数据
  - _import_universities() - 导入高校数据
  - _import_students() - 导入学生数据

脚本:
✓ import_sample_data.py - 数据导入脚本

测试:
✓ test_sample_data.py - 数据导入测试

验证:
- 高校数据可导入
- 学生数据可导入
- 数据完整性验证

Phase 1.7 完成
EOF
)"
```

### 验收标准

- [ ] `tests/test_sample_data.py` 所有测试通过
- [ ] `scripts/import_sample_data.py` 可成功运行
- [ ] 示例高校数据导入成功（至少 5 所）
- [ ] 示例学生数据导入成功（至少 2 个）
- [ ] 数据可在数据库中查询

---

## Phase 1 总结

### 完成情况

| 任务 | 状态 | 输出物 |
|------|------|--------|
| 1.1 创建 uv 项目 | ✅ | pyproject.toml, .python-version |
| 1.2 安装依赖 | ✅ | uv.lock, Makefile |
| 1.3 创建目录结构 | ✅ | src/, data/, tests/ 等目录 |
| 1.4 DuckDB Schema | ✅ | schema.py, 6 个数据表 |
| 1.5 Streamlit 框架 | ✅ | main.py, config.toml |
| 1.6 页面导航 | ✅ | 5 个页面文件 |
| 1.7 示例数据导入 | ✅ | 示例数据，导入脚本 |

### 验收清单

- [ ] 所有 7 个任务完成
- [ ] 所有测试通过
- [ ] 所有 Git 提交完成
- [ ] 项目可运行：`uv run streamlit run src/edu_info/main.py`
- [ ] 数据库可初始化：`uv run python scripts/setup_db.py`
- [ ] 示例数据可导入：`uv run python scripts/import_sample_data.py`

### 下一步

**Phase 2: 数据收集**（第 2-4 周）
- 收集全国 985/211 高校信息
- 收集 2021-2025 辽宁分数线
- 收集政策文件
- 完善数据导入工具

---

**文档创建完成**  
*创建日期：2026-04-11*
